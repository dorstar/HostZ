# Changelog

## v1.0.1 (2026-05-12)

### 新增

- **删除重复记录**：设置中新增复选框，写入系统 hosts 前自动去除重复的 (IP, hostname) 记录
- **Cron 心跳事件**：每次调度检查周期发送 `cron_tick` 事件，便于诊断后台刷新状态
- **自动检查更新**：设置中新增开关和手动检查按钮，通过 GitHub Releases API 检测新版本，有更新时弹出对话框并提供下载链接

### 修复

- **手动刷新后自动应用到系统**：`refresh_remote` 命令现在会在远程内容拉取成功后自动调用 `apply_to_system`，之前刷新只更新数据库不写系统 hosts
- **Cron 错误可视化**：定时刷新失败时不再只输出 stderr，改为发送 `cron_error` 事件到前端
- **Windows 提权写入失败**：修复 `elevate_copy` 用环境变量传路径在提权边界丢失的问题，改用临时 `.bat` 文件传路径
- **删除重复记录复选框不保存状态**：修复设置面板重新打开时复选框始终显示未选中（HTML 模板缺少 `checked` 绑定）
- **卸载时不删除用户数据**：添加 NSIS 卸载 hook 删除 `%APPDATA%\HostZ\`，修复 Tauri v2 模板 `!ifmacrodef` 直接匹配宏名（不用 `!define` 间接法）导致 hook 未执行的问题
- **去重函数尾部多余换行**：`remove_duplicate_lines` 不再在末尾无换行的内容后额外添加换行
- **去重函数跳过重复行产生空行**：修复当某行被跳过时已写入的 `\n` 分隔符导致的多余空行
- **Cron 未刷新已关闭的远程条目**：原代码错误检查 `item.on` 导致关闭的远程条目被跳过，而引用它的组合条目因此获取不到更新内容。移除 `!item.on` 检查，与原 SwitchHosts 行为一致
- **移除未使用的 import**：`integration_test.rs` 中清理了 `HostsType` 警告

## v1.0.0 (2026-05-11)

### 初始发布

SwitchHosts 的 Rust + Tauri v2 轻量化重写。二进制 10 MB，内存 10 MB，启动 < 1s。

### 功能

**条目管理**
- 三种条目类型：本地（可编辑）、远程（HTTP 拉取）、组合（聚合多个条目）
- 开关切换（单选/多选模式）+ 应用到系统 hosts
- 追加/覆盖两种写入模式
- 右键菜单：编辑、复制、刷新远程、移入回收站
- 回收站：软删除、恢复、永久删除、清空
- 左面板空白处右键快速添加

**远程同步**
- HTTP 拉取远程 hosts 内容（30s 超时）
- 定时自动刷新（cron 60s 检查 + 可选间隔 5min ~ 3d）
- 刷新后自动应用到系统 hosts
- 上次刷新时间显示（topbar）
- 内容未变化时跳过写入

**编辑器**
- 本地类型可编辑，双层叠加实时语法高亮（注释绿色、IP 蓝色）
- 只读类型（远程/组合/SystemHosts）完整语法高亮
- 查找替换（字面搜索、忽略大小写、跨条目）
- 查找结果表头：匹配内容 / 所在条目 / 行号
- 编辑器右键菜单：复制、粘贴、全选
- Tab 键插入缩进
- 选中文字反白

**界面**
- 浅色 / 深色主题（35+ CSS 变量，`color-scheme: dark`）
- 中英双语（设置切换，工具栏/对话框/抽屉/托盘/Toast/右键菜单全部跟随）
- Octicons SVG 图标（16 个，CSS mask-image 自动反白）
- 左侧面板宽度可拖拽调整（180-500px）
- Toast 通知系统（成功/错误，2.5s 自动消失）
- 系统托盘：关闭窗口最小化到托盘，右键菜单（显示/偏好设置/退出）
- 添加/编辑对话框表单优化（横排单选框、宽度统一）

**设置**
- 外观：主题、语言、面板宽度
- 写入：追加/覆盖模式、选择模式（单选/多选）、历史记录上限
- 数据：导入/导出（原生文件对话框）、写入历史查看
- 关于：版本号 + GitHub 链接

**数据管理**
- 写入历史记录（追加/覆盖模式标签）
- 导入导出 JSON
- 数据目录：`%APPDATA%\HostZ\`
- 数据库：`hostz.db` + `cfg.db`

**安全**
- 文件读写路径白名单
- 配置写入白名单（14 个允许键）
- 导入数据验证（空导入拒绝）
- 临时文件 UUID 随机命名（防 TOCTOU）
- 系统路径硬编码（不依赖 `windir` 环境变量）
- 提权写入后验证（读回比对防假成功）
- SQL 参数化查询（零 SQL 注入）

### 移除的 SwitchHosts 功能

- 文件夹类型（由组合类型替代）
- HTTP API（无认证 + CORS `*`，安全风险）
- 代理设置（未实现）
- 全局快捷键（未注册）
- 自动更新检查（未接入 Tauri updater）

### 技术栈

| 层 | 技术 |
|---|------|
| 应用壳 | Tauri v2 |
| 前端 | 纯 HTML/CSS/JS（零框架） |
| 数据库 | SQLite (rusqlite bundled) |
| HTTP | ureq |
| 文件对话框 | tauri-plugin-dialog |
| 图标 | Octicons 16px SVG |

### 构建

```bash
cargo tauri build  # 输出 NSIS 安装包 ~3 MB
```

### 系统要求

- Windows 10+ (WebView2 预装)
- macOS / Linux (需安装 WebView2)

### 测试

38 项（17 单元 + 21 集成），全部通过。
